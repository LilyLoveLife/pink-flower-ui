{
  "name": "pink-flower-ui-all",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "ci:version": "pnpm changesets version",
    "ci:publish": "pnpm publish -r",
    "preinstall": "npx only-allow pnpm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "antd": "^5.4.7",
    "only-allow": "^1.1.1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "husky": "^8.0.0"
  },
  "workspaces": ["packages/*"]
}
